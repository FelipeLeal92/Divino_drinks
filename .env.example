# Porta em que o servidor irá rodar
PORT=3000

# --- Configuração de E-mail (Opcional) ---
# Se estas variáveis não forem preenchidas, os leads serão salvos apenas
# no banco de dados, sem notificação por e-mail.

# Servidor SMTP (ex: smtp.gmail.com, smtp.sendgrid.net)
SMTP_HOST=
# Porta do servidor SMTP (ex: 465 para SSL, 587 para TLS)
SMTP_PORT=
# Usuário de autenticação no servidor SMTP (geralmente seu e-mail)
SMTP_USER=
# Senha de autenticação (use uma "senha de aplicativo" se estiver usando Gmail)
SMTP_PASS=
# E-mail que receberá as notificações de novos leads
MAIL_TO=
# E-mail que aparecerá como remetente (opcional, se não preenchido usa o SMTP_USER)
MAIL_FROM=